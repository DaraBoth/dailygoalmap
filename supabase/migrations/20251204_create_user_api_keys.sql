-- This migration is skipped as the api_keys table already exists
-- The existing api_keys table structure:
-- - id, user_id, key_name, key_type, key_value, is_default, created_at, updated_at
-- This table will be used for storing user API keys for the AI agent

